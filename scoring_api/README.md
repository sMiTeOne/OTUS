# Запуск
```
python api.py
```

Параметры запуска:
- `--log [LOG]` - путь для записи логов (по умолчанию: `None`)
- `--port [PORT]` - порт HTTP-сервера (по умолчанию: `8080`)


# Запуск тестов
```
pytest test.py
```
